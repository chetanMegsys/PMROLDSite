# <IfModule mod_rewrite.c>
# RewriteEngine on
# RewriteBase /

# RewriteCond %{HTTP_HOST} ^persistencemarketresearch\.com$ [NC]
# RewriteRule ^(.*)$ https://www.persistencemarketresearch.com/$1$2 [L,R=301]

# RewriteCond %{HTTPS} !on
# RewriteRule ^(.*) https://%{HTTP_HOST}/$1$2$3 [L,R=301]

# RewriteCond %{HTTPS} off [OR]
# RewriteCond %{HTTP_HOST} !^www\. [NC]
# RewriteCond %{HTTP_HOST} ^(?:www\.)?(.+)$ [NC]
# RewriteRule ^ https://www.%1%{REQUEST_URI} [L,NE,R=301]

# RewriteCond %{HTTPS} off
# ## First rewrite to HTTPS:
# ## Don't put www. here. If it is already there it will be included, if not
# ## the subsequent rule will catch it.
# RewriteRule .* https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
# ## Now, rewrite any request to the wrong domain to use www.
# RewriteCond %{HTTP_HOST} !^www\.
# RewriteRule .* https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# # Remove trailing slashes
# RewriteRule ^(.+)/$ https://%{HTTP_HOST}/$1 [R=301,L]

# # if any use /index.html
# #RewriteRule ^index\.html$ / [R=301,L]

# # Removes index.php from ExpressionEngine URLs
# RewriteCond %{THE_REQUEST} ^GET.*index\.php [NC]
# RewriteCond %{REQUEST_URI} !/system/.* [NC]
# RewriteRule (.*?)index\.php/*(.*) /$1 [R=301,NE,L]

# # Directs all EE web requests through the site index file
# RewriteCond $1 !^(index\.php|assets|uploaded_files|img_cache|jscal|robots\.txt|favicon\.ico)
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)$ /index.php?/$1$2 [L,QSA]

# </IfModule>
# <IfModule mod_mime.c>
#   AddType image/webp .webp
# </IfModule>
# <FilesMatch "\.(gif|jpg|jpeg|png|svg|ico|css|js|swf|webp)$">
#   Header set Cache-Control "max-age=950400, public, must-revalidate"
# </FilesMatch>

# <IfModule mod_headers.c>
#   <FilesMatch "\.(js|css|xml|gz)$">
#     Header append Vary: Accept-Encoding
#   </FilesMatch>
# </IfModule>

# <FilesMatch "\.(ico|pdf|flv|jpg|jpeg|png|gif|svg|js|css|swf)(\.gz)?$">
# Header unset ETag
# FileETag None
# </FilesMatch>

# # compress text, html, javascript, css, xml:
# AddOutputFilterByType DEFLATE text/plain
# AddOutputFilterByType DEFLATE text/html
# AddOutputFilterByType DEFLATE text/xml
# AddOutputFilterByType DEFLATE text/css
# AddOutputFilterByType DEFLATE text/javascript
# AddOutputFilterByType DEFLATE application/xml
# AddOutputFilterByType DEFLATE application/xhtml+xml
# AddOutputFilterByType DEFLATE application/rss+xml
# AddOutputFilterByType DEFLATE image/svg+xml
# AddOutputFilterByType DEFLATE application/javascript
# AddOutputFilterByType DEFLATE application/x-javascript
# AddOutputFilterByType DEFLATE application/x-font-truetype
# AddOutputFilterByType DEFLATE application/x-font-ttf
# AddOutputFilterByType DEFLATE font/ttf
# AddOutputFilterByType DEFLATE application/font-woff2
# AddOutputFilterByType DEFLATE font/woff2


# # Or, compress certain file types by extension:
# <files *.html>
# SetOutputFilter DEFLATE
# </files>
# #Header set Strict-Transport-Security "max-age=31536000" env=HTTPS
# #Header set Access-Control-Allow-Origin https://www.persistencemarketresearch.com
# #<IfModule mod_headers.c>
# #Header set Strict-Transport-Security "max-age=10886400; includeSubDomains; preload" env=HTTPS
# #<FilesMatch "\.(css|gif|ico|jpeg|jpg|js|png|woff)$">
# #Header unset Strict-Transport-Security
# #</FilesMatch>
# #</IfModule>
# #<IfModule mod_headers.c>
# #Header set X-Frame-Options "SAMEORIGIN"
# #</IfModule>
# #<IfModule mod_headers.c>
# #Header set X-Content-Type-Options "nosniff"
# #</IfModule>
# #<IfModule mod_headers.c>
# #Header append X-XSS-Protection: "1; mode=block;"
# #</IfModule>
# #<IfModule mod_headers.c>
# #Referrer-Policy: strict-origin-when-cross-origin
# #Referrer-Policy: same-origin
# #Content-Security-Policy: default-src 'https://www.persistencemarketresearch.com'
# #</IfModule>
# #Header always set Referrer-Policy "same-origin"
# #Content-Security-Policy: default-src 'https://www.persistencemarketresearch.com'
# #Header set Content-Security-Policy "default-src https://www.persistencemarketresearch.com"
# #Header set Content-Security-Policy "script-src https://www.persistencemarketresearch.com"
# #Header always set Content-Security-Policy "default-src 'self' *.google-analytics.com *.paypal.com; script-src 'self' 'unsafe-inline' 'unsafe-eval' *.google-analytics.com; style-src 'self' 'unsafe-inline'; img-src data: *; object-src 'none'"
# #Header always set Permissions-Policy "accelerometer=(),autoplay=(),camera=(),encrypted-media=(),fullscreen=*,geolocation=*,gyroscope=(),interest-cohort=(),magnetometer=(),microphone=(),midi=(),sync-xhr=*,usb=()"
# #Header edit Set-Cookie ^(.*)$ $1;HttpOnly;Secure;SameSite=strict



<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /PMRweb/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
